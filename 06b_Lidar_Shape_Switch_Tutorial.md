# 6-2 부록: LiDAR 형상 변경 - lds_1.stl로 LDS-02 / LDS-03 형상 적용

> **목적**: `Assets\URDF\meshes\sensors\` 에 추가한 `lds_1.stl` 메시를 씬의 TurtleBot3 LiDAR 형상으로 교체한다.
> **전제 조건**: 06단계 완료 + `Assets\Editor\LDSSensorSwitcher.cs` (스위처 도구) 존재
> **소요 시간**: 메시 임포트까지 포함 약 5 ~ 10분

---

## 이 문서의 목표

06단계에서 임포트된 TurtleBot3의 LiDAR는 기본적으로 **LDS-01 클래식 원통형**(`lds.stl`)입니다.
여기에 `lds_1.stl`(직접 확보한 LDS-02=LD08 원반형 또는 LDS-03=COIN-D4 각형)을 적용하는 방법을 정리합니다.

| 질문 | 답 |
|------|-----|
| 파일만 폴더에 넣으면 자동으로 바뀌나? | ❌ **아니요**. 에디터 도구로 "적용" 버튼을 눌러야 바뀝니다. |
| `lds_0.stl`을 지우고 `lds_1.stl`을 `lds_0.stl`로 이름만 바꿔도 되나? | ❌ **안 됩니다**. 이유는 1-2절 참고 (씬은 파일명이 아니라 메시 에셋 guid를 참조). |
| 다시 LDS-01로 되돌릴 수 있나? | ✅ 스위처의 [URDF 기본(LDS-01)으로 복원] 버튼으로 1초 만에 복원. |

---

## 목차

1. [구조 이해: 왜 이름 바꾸기가 안 되는가](#1-구조-이해-왜-이름-바꾸기가-안-되는가)
2. [준비: lds_1.stl 임포트 확인](#2-준비-lds_1stl-임포트-확인)
3. [스위처로 형상 적용](#3-스위처로-형상-적용)
4. [보정: 크기 / 방향 / 위치](#4-보정-크기--방향--위치)
5. [(선택) lds.prefab을 직접 수정해 기본 형상으로 고정](#5-선택-ldsprefab을-직접-수정해-기본-형상으로-고정)
6. [되돌리기와 문제 해결](#6-되돌리기와-문제-해결)

---

## 1. 구조 이해: 왜 이름 바꾸기가 안 되는가

씬의 LiDAR는 **STL 파일명**이 아니라 아래 참조 체인을 따라 메시 에셋을 사용합니다.

```text
씬(SampleScene)
 └─ lds 프리팹 인스턴스   (guid 868583a6…)   ← 이름만 "lds"
    └─ lds_0 자식        (MeshFilter)
       └─ 메시 에셋 lds_0.asset (guid a85bd553…)  ← ★ 실제로 그려지는 메시 ★
```

- `lds_1.stl`을 아무리 `lds_0.stl`로 바꿔도 **다른 파일(새 guid)** 이 되므로 씬 참조는 전혀 변경되지 않습니다.
- 그리고 폴더 안에 `lds_0.stl` 파일은 존재하지 않으며, `lds_0.asset`을 지우면 오히려 참조가 깨져 **형상이 사라집니다**.

> ✅ 그래서 올바른 방법은 **씬의 MeshFilter가 그리도록 "적용" 버튼으로 새 메시를 지정**하는 것입니다 (3절).

---

## 2. 준비: lds_1.stl 임포트 확인

`lds_1.stl`은 단순히 폴더에 넣은 것만으로는 아직 **Unity에 임포트되지 않았습니다** (임포트 전에는 `.meta` 파일이 없음).

**확인 방법:**

```text
Assets\URDF\meshes\sensors\
├─ lds_1.stl          ← 방금 추가한 파일
└─ lds_1.stl.meta    ← ★ 이 파일이 생겨야 임포트 완료 ★
```

1. **Unity 에디터 창으로 클릭/포커스** → 자동으로 임포트가 시작됩니다.
2. 임포트 완료 후 프로젝트 창에서 `lds_1.stl`을 클릭하면 **Inspector에 Mesh**가 표시됩니다.
   - 이때 Mesh의 모습을 미리 확인할 수 있고, **File Scale** 설정이 비어 있으면 **0.001**로 지정해 두면 mm → m 변환이 적용됩니다 (권장).
3. `lds_1.stl.meta`가 안 생기면 Unity를 새로 포커스 하거나 `Ctrl+R`(Refresh)로 다시 임포트를 시킵니다.

> 💡 **배치 위치는 반드시** `Assets\URDF\meshes\sensors\` 여야 합니다 (스위처가 이 폴더를 자동 감지).

---

## 3. 스위처로 형상 적용

메뉴 **Tools > TurtleBot3 > LDS Sensor Switcher** 창을 엽니다.
(Play가 켜져 있으면 동작하지 않으므로 **에디터(편집) 모드**에서 실행하세요.)

```text
[ 1. 대상 찾기 ]
   "씬에서 lds 노드 자동 찾기" 클릭
   → "발견된 경로"에  .../base_scan/lds  표시 확인 ✓

[ 2. 모델 적용 ]
   LDS-02 또는 LDS-03 프리셋(둘 중 아무거나)을 펼침
   → 프리셋의 [Mesh] 필드에 "lds_1" 메시가 자동으로 채워졌는지 확인
      (안 채워지면 Project 창의 lds_1.stl을 Mesh 필드로 드래그)
   → [이 모델로 적용] 클릭

[ 3. 확인 / 저장 ]
   Scene 뷰에서 base_scan 위 형상이 바뀐 것을 확인
   → Ctrl+S 로 씬 저장
```

**프리셋 선택이 헷갈릴 때:** LDS-02 / LDS-03 프리셋 차이는 표시 이름과 보정 기본값뿐입니다.
`lds_1.stl`이 어떤 모델이든 둘 중 하나를 쓰면 되고, 최종 모습은 4절 보정 값으로 맞추면 됩니다.

> ⚠️ [이 모델로 적용]을 눌렀는데 "메시 파일이 없습니다"가 뜬다면 **2절 임포트가 아직 끝나지 않은 것**입니다.

---

## 4. 보정: 크기 / 방향 / 위치

교체 후 모양이 어긋나면 해당 프리셋의 보정 값을 수정하고 다시 [이 모델로 적용]을 누릅니다.

| 항목 | 기본값 | 언제 바꾸나 | 예시 |
|------|--------|-------------|------|
| `scale` | 0.001 | 외부 STL이 mm 단위면 그대로 / m 단위면 1로 | 센서가 너무 크면 0.0005로 줄임 |
| `rotation` (deg) | 0 | 메시가 돌아가 있으면 90, 180씩 | 바닥이 위로 오면 X회전 180 |
| `position` (m) | 0 | 센서 높이/위치가 대충 맞지 않을 때 미세 조정 | 위로 0.005 올리기 |

- Unity STL 임포트 대화창에서 이미 **File Scale = 0.001** 을 설정했다면 **프리셋 scale은 1**로 두세요 (이중 축소 방지).
- 보정 권장 순서: **① scale(크기) → ② rotation(방향) → ③ position(높이/위치)**. 각 단계마다 Scene 뷰로 눈금을 확인하며 맞춥니다.

---

## 5. (선택) lds.prefab을 직접 수정해 기본 형상으로 고정

스위처 적용은 "씬을 저장하면 씬에 반영"되는 방식이라 **로봇이 2대 이상이면 각각 적용**해야 합니다.
대신 아래처럼 **프리팹 자체를 수정**하면 프로젝트의 모든 씬/인스턴스에 새 형상이 기본으로 적용됩니다.

1. Project 창에서 `Assets\URDF\meshes\sensors\lds.prefab` **더블클릭** (프리팹 편집 모드 진입)
2. Hierarchy에서 `lds > lds_0` 자식 선택 → Inspector의 **Mesh Filter → Mesh**를
   임포트된 `lds_1` 메시로 드래그 교체
3. 상단 **Apply** 클릭 → 프리팹 저장

> ⚠️ 이 방식은 "원본(LDS-01) 형상" 자체를 새 메시로 바꾸는 것이므로,
> 이후 스위처의 **[URDF 기본(LDS-01)으로 복원]** 도 새 메시 기준으로 복원됩니다.
> 원통형(LDS-01)으로 되돌리려면 이 방법 대신 **스위처 적용 방식(3절)** 이 좋습니다.

---

## 6. 되돌리기와 문제 해결

**다시 LDS-01 원통형으로 되돌리기:** 스위처 창에서 **[URDF 기본(LDS-01)으로 복원]** 클릭.

| 증상 | 원인 / 대책 |
|------|-------------|
| [이 모델로 적용] 시 "메시 파일이 없습니다" | `lds_1.stl`이 아직 임포트 안 됨 → 2절 (Unity 포커스 / Refresh) |
| 형상이 엄청 크게 보임 | mm → m 미변환 → 프리셋 `scale` 0.001 확인 (또는 File Scale 0.001) |
| 형상이 거꾸로/옆으로 뒤집힘 | 프리셋 `rotation` 을 90 / 180 단위로 조정 |
| Scene 뷰에서 안 보임 | `lds_1.stl` 임포트 실패 여부, `base_scan > lds` 노드 존재, 형상이 스케일 1 기준 너무 작을 가능성 |
| Play(실행) 중에 버튼이 눌리지 않음 | 스위처는 에디터 전용 → Play를 중지하고 실행 |
| 컴파일 오류(CScaru,…) | Assets\Editor의 스위처 스크립트 저장·재컴파일, 콘솔 오류 문구 확인 후 재시도 |

---

## 최종 파일 구조

```text
TurtleBot3\Assets\URDF\meshes\sensors\
├─ lds.stl             ← 기존 (LDS-01 원본, 건드리지 않음)
├─ lds_0.asset         ← 씬이 참조하는 기본 메시 에셋 (삭제 금지)
├─ lds.prefab          ← lds 프리팹 (3절 스위처 적용 시 수정 대상은 아님)
├─ lds_1.stl           ← 추가한 새 메시 (형상 교체 소스)
├─ lds_1.stl.meta      ← Unity 임포트 완료 후 자동 생성
└─ (LDS-02/03 확보 시 동일 위치에 추가 가능)

TurtleBot3\Assets\Editor\
└─ LDSSensorSwitcher.cs  ← 형상 교체 에디터 도구 (스위처)
```

> **관련 자료**: 06단계 목차의 [10. 부록: LiDAR 형상 변경]과 같은 방법을 다룹니다. 이 문서는 `lds_1.stl` 파일 하나로 바로 적용하는 절차에 초점을 맞춘 실전 가이드입니다.